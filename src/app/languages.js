import en from './constants/en.json';
import ar from './constants/ar.json';

export const dictionaryList = { en, ar };

// export const languageOptions = {
//   en: 'English',
//   ar: 'العربية'
// };
// we can use this to add more than 2 languages and a spinner list
